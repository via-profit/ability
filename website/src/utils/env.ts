/**
 * Version of the documented library (injected by webpack from the package.json)
 */
export const ABILITY_VERSION = process.env.ABILITY_VERSION || '';

/**
 * Base path of the site (for example, `/ability/` for GitHub Pages of the project)
 */
export const PUBLIC_PATH = process.env.PUBLIC_PATH || '/';

export const GITHUB_URL = 'https://github.com/via-profit/ability';

export const NPM_URL = 'https://www.npmjs.com/package/@via-profit/ability';
